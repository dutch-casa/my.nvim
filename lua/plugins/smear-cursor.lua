return {
  "sphamba/smear-cursor.nvim",
  opts = {
    stiffness = 0.5,
    trailing_stiffness = 0.35,
    trailing_exponent = 5,
    max_length = 35,
    legacy_computing_symbols_support = true,
  },
}
